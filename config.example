# gh-claudecode configuration
# Copy this file to .env and edit as needed

# Target GitHub repository (owner/repo)
REPO="jpfaria/OpenRig"

# Path to local repo (optional, avoids cloning for solver)
# REPO_DIR="/Users/joao.faria/Projetos/github.com/jpfaria/OpenRig"

# Refiner polling interval in seconds (default: 300)
REFINER_INTERVAL=300

# Solver polling interval in seconds (default: 600)
SOLVER_INTERVAL=600

# Max time in seconds for in-progress before marking as failed (default: 3600)
SOLVER_TIMEOUT=3600

# Claude model to use (default: claude-sonnet-4-6)
CLAUDE_MODEL="claude-sonnet-4-6"
